# time_tracker_mobile

Minimalna działająca aplikacja Flutter pod iOS z flavorami (`dev`, `staging`, `prod`) i strukturą Clean Architecture.

## Uruchomienie

```bash
flutter pub get
flutter run -t lib/main_dev.dart
```
